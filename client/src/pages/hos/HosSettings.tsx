import { Settings } from "lucide-react";
import HosComingSoon from "./HosComingSoon";

export default function HosSettings() {
  return <HosComingSoon titleKey="hos_settings" descKey="hos_settings_desc" icon={Settings} />;
}
